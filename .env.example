# ── Databricks authentication ─────────────────────────────────────────────────
# Optional: set a Databricks CLI configuration profile to use for authentication.
# If not set, the SDK uses the DEFAULT profile or ambient credentials.
# Run `databricks auth login --profile <name>` to create a profile.
# DATABRICKS_CONFIG_PROFILE=<your-profile-name>

# ── Lakebase (PostgreSQL) connection ──────────────────────────────────────────
# These are required for the app to connect to your Databricks Lakebase instance.
# When deployed as a Databricks App with a Lakebase resource attached, the PG*
# variables are set automatically by the platform.

PGHOST=<your-lakebase-host>
PGDATABASE=databricks_postgres
PGPORT=5432

# Name of your Lakebase instance as shown in the Databricks UI.
# Falls back to PGAPPNAME (set automatically in Databricks Apps) if not provided.
LAKEBASE_INSTANCE_NAME=<your-instance-name>

# ── LLM model (optional) ─────────────────────────────────────────────────────
# Override the Foundation Model Serving endpoint used by the AI chat assistant.
# Defaults to databricks-claude-sonnet-4-5 if not set.
# DATABRICKS_LLM_MODEL=databricks-claude-sonnet-4-5
